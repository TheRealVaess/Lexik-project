{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <div class="container">
    {% for product in products %}
        <div class="row">
            <div class="col-4">
                <p>{{ product.name }}</p>
            </div>
            <div class="col-2">
                <a href="{{ path('products_details', {id: product.id}) }}">Voir détail</a>
            </div>
        </div>
    {% endfor %}
    </div>

{% endblock %}
